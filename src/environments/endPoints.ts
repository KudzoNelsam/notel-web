import {ENVIRONMENT} from './dev-environment';

export const ENDPOINTS = {
  ETABLISSEMENTS : ENVIRONMENT.apiUrl + "/web/etablissements",
  ETABLISSEMENTS_DETAIL : ENVIRONMENT.apiUrl + "/web/etablissements",
  ETABLISSEMENTS_ALEATOIRE : ENVIRONMENT.apiUrl + "/web/etablissements/aleatoire",
}
